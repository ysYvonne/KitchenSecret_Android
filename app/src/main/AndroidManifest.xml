<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.meetyouatnowhere.kitchensecret_android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.registerActivity" />
        <activity android:name=".activities.favorActivity" />
        <activity android:name=".activities.RecipeActivity" />
        <activity android:name=".activities.SortActivity" />
        <activity android:name=".activities.EditPersonalInfoActivity" />
        <activity
            android:name=".activities.PersonSpaceActivity"
            android:label="@string/title_activity_person_space"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.SettingActivity" />
        <activity android:name=".activities.Setting_detailActivity"></activity>
    </application>

</manifest>